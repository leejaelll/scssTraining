{
  "scripts": {
    "start": "run-p serve compile",
    "serve": "live-server public --host=localhost --port=9000 --no-browser",
    "compile": "sass src/sass:public/assets/styles --no-charset --watch --update",
    "clear": "rm -rf public/assets/styles"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "4.1.5"
  }
}
